[pytest]
testpaths = api_tests ui_tests
addopts = -ra --strict-markers
markers =
    api: backend API tests (fast, no browser)
    ui: Selenium UI tests (requires Chrome)
    e2e: end-to-end client-server tests
    smoke: minimal subset for quick health checks
